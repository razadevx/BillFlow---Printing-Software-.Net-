<UserControl x:Class="BillFlow.Views.CustomerDetailPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:BillFlow.Models"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <!-- Notifications -->
            <Border Background="#FEF2F2" BorderBrush="{StaticResource DangerBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,12"
                    Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="AlertCircleOutline" Foreground="{StaticResource DangerBrush}" Width="20" Height="20" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" Margin="8,0,0,0" VerticalAlignment="Center" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
            <Border Background="#F0FDF4" BorderBrush="{StaticResource SuccessBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,12"
                    Visibility="{Binding SuccessMessage, Converter={StaticResource StringToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="CheckCircleOutline" Foreground="{StaticResource SuccessBrush}" Width="20" Height="20" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding SuccessMessage}" Foreground="{StaticResource SuccessBrush}" Margin="8,0,0,0" VerticalAlignment="Center" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

        <Border Style="{StaticResource GlassCardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding RiskEmoji}" FontSize="22" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding Customer.Name}" Style="{StaticResource H2Style}"/>
                    </StackPanel>
                    <TextBlock Text="{Binding Customer.CustomerCode}" Style="{StaticResource BodySmallStyle}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Style="{StaticResource SecondaryButtonStyle}" Content="← Back" Command="{Binding BackToCustomersCommand}" VerticalAlignment="Center"/>
            </Grid>
        </Border>
        </StackPanel>

        <TabControl Grid.Row="1" BorderThickness="0" Background="Transparent">
            <TabItem Header="Profile">
                <ScrollViewer Style="{StaticResource ModernScrollViewerStyle}" Padding="0,8,0,0">
                    <StackPanel>
                        <Border Style="{StaticResource GlassCardStyle}" Margin="0,0,0,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="PHONE" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding Customer.Phone}" Style="{StaticResource BodyStyle}" Margin="0,0,0,12"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="CREDIT LIMIT" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Customer.CreditLimit, StringFormat='PKR {0:N0}'}" Style="{StaticResource NumberStyle}" Margin="0,0,0,12"/>
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="OUTSTANDING" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Grid.Row="3" Grid.Column="0" Text="{Binding Customer.TotalCredit, StringFormat='PKR {0:N2}'}" Style="{StaticResource NumberStyle}" Foreground="{StaticResource PrimaryBrush}"/>
                                <TextBlock Grid.Row="2" Grid.Column="1" Text="TODAY'S PENDING" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding TodayPending, StringFormat='PKR {0:N2}'}" Style="{StaticResource NumberStyle}"/>
                            </Grid>
                        </Border>
                        <Border Style="{StaticResource GlassCardStyle}">
                            <StackPanel>
                                <TextBlock Text="ADDRESS" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Text="{Binding Customer.Address}" Style="{StaticResource BodyStyle}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Work History">
                <DataGrid ItemsSource="{Binding WorkOrders}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          BorderThickness="0"
                          Background="Transparent"
                          Margin="0,8,0,0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ORDER" Binding="{Binding OrderCode}" Width="120"/>
                        <DataGridTextColumn Header="DATE" Binding="{Binding OrderDate, StringFormat='{}{0:dd MMM yyyy}'}" Width="100"/>
                        <DataGridTextColumn Header="STATUS" Binding="{Binding WorkStatus}" Width="100"/>
                        <DataGridTextColumn Header="PAYMENT" Binding="{Binding PaymentStatus}" Width="90"/>
                        <DataGridTextColumn Header="TOTAL" Binding="{Binding GrandTotal, StringFormat='PKR {0:N2}'}" Width="*"/>
                        <DataGridTextColumn Header="PENDING" Binding="{Binding PendingAmount, StringFormat='PKR {0:N2}'}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Payment History">
                <DataGrid ItemsSource="{Binding PaymentKhataEntries}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          Margin="0,8,0,0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="DATE" Binding="{Binding TransactionDate, StringFormat='{}{0:dd MMM yyyy HH:mm}'}" Width="140"/>
                        <DataGridTextColumn Header="AMOUNT" Binding="{Binding Amount, StringFormat='PKR {0:N2}'}" Width="100"/>
                        <DataGridTextColumn Header="BALANCE AFTER" Binding="{Binding Balance, StringFormat='PKR {0:N2}'}" Width="120"/>
                        <DataGridTextColumn Header="NOTES" Binding="{Binding Notes}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Credit History">
                <DataGrid ItemsSource="{Binding AllKhataEntries}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          Margin="0,8,0,0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="DATE" Binding="{Binding TransactionDate, StringFormat='{}{0:dd MMM yyyy HH:mm}'}" Width="140"/>
                        <DataGridTextColumn Header="TYPE" Binding="{Binding Type}" Width="80"/>
                        <DataGridTextColumn Header="AMOUNT" Binding="{Binding Amount, StringFormat='PKR {0:N2}'}" Width="100"/>
                        <DataGridTextColumn Header="BALANCE" Binding="{Binding Balance, StringFormat='PKR {0:N2}'}" Width="100"/>
                        <DataGridTextColumn Header="NOTES" Binding="{Binding Notes}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
