<UserControl x:Class="BillFlow.Views.DailySchedulePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Daily Schedule" FontSize="32" FontWeight="Bold" Foreground="{StaticResource Gray900Brush}" FontFamily="{StaticResource InterFontFamily}"/>
            <TextBlock Text="Plan your day and track scheduled printing jobs." FontSize="14" Foreground="{StaticResource Gray500Brush}" Margin="0,8,0,0" FontFamily="{StaticResource InterFontFamily}"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource GlassCardStyle}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Style="{StaticResource SecondaryButtonStyle}" Padding="12"
                            Command="{Binding PreviousDayCommand}">
                        <materialDesign:PackIcon Kind="ChevronLeft" Width="20" Height="20"/>
                    </Button>

                    <Border Style="{StaticResource GlassCardStyle}" Padding="12,8" Margin="8,0">
                        <DatePicker BorderThickness="0" Background="Transparent"
                                    SelectedDate="{Binding SelectedDate, Mode=TwoWay}"/>
                    </Border>

                    <Button Style="{StaticResource SecondaryButtonStyle}" Padding="12"
                            Command="{Binding NextDayCommand}">
                        <materialDesign:PackIcon Kind="ChevronRight" Width="20" Height="20"/>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Border Style="{StaticResource GlassCardStyle}" Padding="12,8" Margin="0,0,8,0">
                        <TextBlock Text="{Binding TotalSqFt, StringFormat=Total SqFt: {0:F1}}" Style="{StaticResource NumberStyle}"/>
                    </Border>
                    <Border Style="{StaticResource GlassCardStyle}" Padding="12,8">
                        <TextBlock Text="{Binding TotalRevenue, StringFormat=Revenue collected: PKR {0:N2}}" Foreground="{StaticResource SuccessBrush}" Style="{StaticResource NumberStyle}"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource GlassCardStyle}" Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="CheckCircle" Width="24" Height="24" Foreground="{StaticResource SuccessBrush}"/>
                    <TextBlock Style="{StaticResource BodyStyle}" Margin="12,0,0,0" VerticalAlignment="Center">
                        <Run Text="Done: "/>
                        <Run Text="{Binding DoneCount, Mode=OneWay}"/>
                        <Run Text=" items"/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource GlassCardStyle}" Margin="8,0,0,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ClockOutline" Width="24" Height="24" Foreground="{StaticResource WarningBrush}"/>
                    <TextBlock Style="{StaticResource BodyStyle}" Margin="12,0,0,0" VerticalAlignment="Center">
                        <Run Text="To-Do: "/>
                        <Run Text="{Binding TodoCount, Mode=OneWay}"/>
                        <Run Text=" items"/>
                    </TextBlock>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource GlassCardStyle}" Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="✅ Done" Style="{StaticResource H3Style}" Margin="0,0,0,16" Foreground="{StaticResource SuccessBrush}"/>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding CompletedWork}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource GlassCardHoverableStyle}" Margin="0,0,0,8">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="{Binding Customer.Name}" Style="{StaticResource BodyStyle}" FontWeight="SemiBold"/>
                                            <TextBlock Grid.Row="1" Text="{Binding Notes, TargetNullValue={x:Static sys:String.Empty}, FallbackValue=—}"
                                                       Style="{StaticResource BodySmallStyle}" Margin="0,2" TextTrimming="CharacterEllipsis"/>
                                            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,4,0,0">
                                                <TextBlock Style="{StaticResource BodySmallStyle}" Foreground="{StaticResource PrimaryBrush}">
                                                    <Run Text="{Binding TotalArea, StringFormat=F1}"/><Run Text=" SqFt"/>
                                                </TextBlock>
                                                <TextBlock Text=" • " Style="{StaticResource BodySmallStyle}" Foreground="{StaticResource Gray400Brush}"/>
                                                <TextBlock Text="{Binding GrandTotal, StringFormat=PKR {0:N2}}" Style="{StaticResource BodySmallStyle}" FontWeight="SemiBold"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource GlassCardStyle}" Margin="8,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="⏳ To-Do" Style="{StaticResource H3Style}" Margin="0,0,0,16" Foreground="{StaticResource WarningBrush}"/>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding PendingWork}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource GlassCardHoverableStyle}" Margin="0,0,0,8">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Text="{Binding Customer.Name}" Style="{StaticResource BodyStyle}" FontWeight="SemiBold"/>
                                            <TextBlock Grid.Row="1" Text="{Binding Notes, TargetNullValue={x:Static sys:String.Empty}, FallbackValue=—}"
                                                       Style="{StaticResource BodySmallStyle}" Margin="0,2" TextTrimming="CharacterEllipsis"/>
                                            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,4,0,0">
                                                <TextBlock Style="{StaticResource BodySmallStyle}" Foreground="{StaticResource PrimaryBrush}">
                                                    <Run Text="{Binding TotalArea, StringFormat=F1}"/><Run Text=" SqFt"/>
                                                </TextBlock>
                                                <TextBlock Text=" • " Style="{StaticResource BodySmallStyle}" Foreground="{StaticResource Gray400Brush}"/>
                                                <TextBlock Text="{Binding GrandTotal, StringFormat=PKR {0:N2}}" Style="{StaticResource BodySmallStyle}" FontWeight="SemiBold"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="1" Grid.RowSpan="3" Orientation="Vertical" VerticalAlignment="Center">
                                                <Button Style="{StaticResource PrimaryButtonStyle}" Content="Mark Done" Padding="12,6" Margin="0,0,0,4"
                                                        Command="{Binding DataContext.MarkAsDoneCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"/>
                                                <Button Style="{StaticResource SecondaryButtonStyle}" Content="Reschedule" Padding="12,6" Margin="0,0,0,4"
                                                        Command="{Binding DataContext.RescheduleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"/>
                                                <Button Style="{StaticResource SecondaryButtonStyle}" Content="Details" Padding="12,6"
                                                        Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
