<Window x:Class="BillFlow.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:BillFlow.ViewModels"
        xmlns:converters="clr-namespace:BillFlow.Converters"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="BillFlow — Printing Business Management"
        Height="800" Width="1400"
        MinHeight="640" MinWidth="1100"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource Gray50Brush}"
        FontFamily="{StaticResource InterFontFamily}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0"
                Background="White"
                BorderBrush="{StaticResource Gray200Brush}"
                BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Brand -->
                <Border Grid.Row="0"
                        Padding="24,28,24,20"
                        BorderBrush="{StaticResource Gray200Brush}"
                        BorderThickness="0,0,0,1">
                    <StackPanel Orientation="Horizontal">
                        <Border Width="44" Height="44" CornerRadius="12"
                                Background="{StaticResource PrimaryBrush}">
                            <materialDesign:PackIcon Kind="PrinterOutline" Width="26" Height="26"
                                                     Foreground="White"
                                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="BillFlow" Style="{StaticResource H2Style}" FontSize="22"/>
                            <TextBlock Text="Print &amp; Credit" Style="{StaticResource BodySmallStyle}" Opacity="0.75" Margin="0,2,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <TextBlock Grid.Row="1"
                           Text="MENU"
                           Style="{StaticResource LabelStyle}"
                           Margin="24,16,24,8"
                           Opacity="0.55"/>

                <!-- Navigation -->
                <ScrollViewer Grid.Row="2"
                              VerticalScrollBarVisibility="Auto"
                              Padding="12,0,0,20">
                    <ItemsControl ItemsSource="{Binding NavigationItems}" Margin="0,0,12,0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:NavItem}">
                                <Button Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding Name}"
                                        HorizontalContentAlignment="Stretch">
                                    <Button.Tag>
                                        <MultiBinding Converter="{StaticResource RouteMatchConverter}">
                                            <Binding Path="DataContext.CurrentRoute" RelativeSource="{RelativeSource AncestorType=Window}"/>
                                            <Binding Path="Name"/>
                                        </MultiBinding>
                                    </Button.Tag>
                                    <Button.Margin>0,0,0,6</Button.Margin>
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="{StaticResource Gray700Brush}"/>
                                            <Setter Property="Padding" Value="4"/>
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                                            <Setter Property="MinHeight" Value="48"/>
                                            <Setter Property="Cursor" Value="Hand"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Grid>
                                                            <!-- Left border accent for active state -->
                                                            <Border x:Name="LeftAccent" 
                                                                    Background="{StaticResource PrimaryBrush}" 
                                                                    Width="3" 
                                                                    CornerRadius="0,2,2,0"
                                                                    HorizontalAlignment="Left"
                                                                    Visibility="Collapsed"/>
                                                            <!-- Main background -->
                                                            <Border x:Name="BgBorder" 
                                                                    Background="{TemplateBinding Background}" 
                                                                    CornerRadius="10"
                                                                    Margin="4,2">
                                                                <ContentPresenter x:Name="ContentPresenter"
                                                                                  Margin="12,10"
                                                                                  HorizontalAlignment="Stretch"
                                                                                  VerticalAlignment="Center"
                                                                                  RecognizesAccessKey="True"/>
                                                            </Border>
                                                        </Grid>
                                                        <ControlTemplate.Triggers>
                                                            <!-- Active State (when Tag is True) -->
                                                            <Trigger Property="Tag" Value="True">
                                                                <Setter TargetName="LeftAccent" Property="Visibility" Value="Visible"/>
                                                                <Setter TargetName="BgBorder" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                                <Setter TargetName="ContentPresenter" Property="TextBlock.Foreground" Value="White"/>
                                                                <Setter TargetName="BgBorder" Property="Effect">
                                                                    <Setter.Value>
                                                                        <DropShadowEffect ShadowDepth="0" BlurRadius="8" Opacity="0.25" Color="#4F46E5"/>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </Trigger>
                                                            <!-- Hover State -->
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="BgBorder" Property="Background" Value="#F3F4F6"/>
                                                            </Trigger>
                                                            <!-- Hover on Active State -->
                                                            <MultiTrigger>
                                                                <MultiTrigger.Conditions>
                                                                    <Condition Property="Tag" Value="True"/>
                                                                    <Condition Property="IsMouseOver" Value="True"/>
                                                                </MultiTrigger.Conditions>
                                                                <Setter TargetName="BgBorder" Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                                                            </MultiTrigger>
                                                            <!-- Pressed State -->
                                                            <Trigger Property="IsPressed" Value="True">
                                                                <Setter TargetName="BgBorder" Property="Background" Value="#E5E7EB"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                    <Grid>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding IconGlyph}"
                                                       FontSize="18"
                                                       Width="28"
                                                       VerticalAlignment="Center"
                                                       TextAlignment="Center">
                                                <TextBlock.Foreground>
                                                    <Binding Path="Tag" RelativeSource="{RelativeSource AncestorType=Button}">
                                                        <Binding.Converter>
                                                            <converters:ActiveNavForegroundConverter/>
                                                        </Binding.Converter>
                                                    </Binding>
                                                </TextBlock.Foreground>
                                            </TextBlock>
                                            <TextBlock Text="{Binding Title}"
                                                       FontFamily="{StaticResource InterFontFamily}"
                                                       FontSize="14"
                                                       FontWeight="SemiBold"
                                                       VerticalAlignment="Center"
                                                       Margin="10,0,0,0">
                                                <TextBlock.Foreground>
                                                    <Binding Path="Tag" RelativeSource="{RelativeSource AncestorType=Button}">
                                                        <Binding.Converter>
                                                            <converters:ActiveNavForegroundConverter/>
                                                        </Binding.Converter>
                                                    </Binding>
                                                </TextBlock.Foreground>
                                            </TextBlock>
                                        </StackPanel>
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Main -->
        <Grid Grid.Column="1">
            <Border
                    Background="{StaticResource Gray50Brush}"
                    Padding="28,20,28,28">
                <Frame x:Name="MainFrame"
                       NavigationUIVisibility="Hidden"
                       Background="Transparent"/>
            </Border>
        </Grid>
    </Grid>
</Window>
